import category from "./local.api/category.api";
import catalog from "./local.api/catalog.api";
import mpage from "./local.api/mpage.api";
import project from "./local.api/project.api";

const api = {
  category,
  catalog,
  mpage,
  project,
};

export default api;
